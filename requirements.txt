# Core framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0

# Licensing — cryptographic
cryptography>=41.0.0
PyJWT>=2.8.0
ntplib>=0.4.0

# Hardware ID — platform-specific
# Windows only (skip on macOS/Linux):
#   pip install wmi pywin32
# These are optional — the system falls back to WMIC/subprocess if missing
wmi>=1.5.1; sys_platform == 'win32'
pywin32>=306; sys_platform == 'win32'

# HTTP client for testing
requests>=2.28.0
